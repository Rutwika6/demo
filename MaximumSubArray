class Solution {
    public int maxSubArray(int[] nums) {
        int sum=0;
        int max=nums[0];
        int min=0;
        for(int n:nums){
            sum+=n;
            max=Math.max(max,sum-min);
            min=Math.min(min,sum);
        }
        return max;
    }
    
}
